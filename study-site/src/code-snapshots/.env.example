# Copy to .env and adjust for non-Docker local development.
# Docker Compose defines its own service-internal URLs, so the API container
# does not read this file.
DATABASE_URL=postgresql+psycopg://book_a_slot:book_a_slot@localhost:5432/book_a_slot
REDIS_URL=redis://localhost:6379/0